# AI 求职助手｜产品需求文档

## 产品目标

帮助求职者根据目标岗位，快速优化简历与求职材料，提升材料与岗位要求的匹配度和表达质量。

## 用户流程

1. 用户上传一份可选中文本的 PDF 简历。
2. 用户粘贴目标岗位描述（JD）。
3. 浏览器提取 PDF 中的文字，并保存在当前页面内存中。
4. 页面将简历文字与岗位描述发送至服务端分析路由。
5. 服务端使用环境变量中的 DeepSeek API Key 请求 DeepSeek API。
6. 页面展示匹配度、优化建议、求职信和面试问答。

## 当前版本范围

- Next.js 单页应用，桌面与移动端响应式展示。
- 浏览器端使用 `pdfjs-dist` 解析 PDF 文本；登录后将原始 PDF 保存至 Supabase 私有 Storage，并保存解析文本。
- 使用 Supabase Auth 邮箱 magic-link 登录；用户可访问自己的简历、岗位与分析历史。
- 服务端路由 `/api/analyze` 调用 DeepSeek OpenAI 兼容接口，并保存分析任务、结构化结果和用量事件。
- AI 结果包括：岗位匹配度分析、3 条简历优化建议、一封求职信、10 个面试问题与参考回答。

## 非功能要求

- 密钥必须配置于 `.env.local` 的 `DEEPSEEK_API_KEY`，不得使用 `NEXT_PUBLIC_` 前缀，也不得写入浏览器代码。
- 简历、岗位描述与 AI 分析结果仅向所属用户开放；用户删除简历时，应同步清理关联文件和分析记录。
- 当 API Key 未配置、PDF 无文本或服务端失败时，给出明确提示。

## 本地运行

1. 复制 `.env.example` 为 `.env.local`，填写 Supabase 的 Project URL、Publishable key、Secret key 与 `DEEPSEEK_API_KEY`；官方地址为 `DEEPSEEK_BASE_URL=https://api.deepseek.com`。
2. 执行 `npm install`。
3. 执行 `npm run dev`，打开终端给出的本地地址。
