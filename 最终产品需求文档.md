# AI 求职助手｜最终产品需求文档

> 产品名称：履历（Career Intelligence）
> 文档状态：已实现的 v1 基线
> 最后更新：2026-08-15

## 1. 产品概述

“履历”是面向求职者的 AI 求职材料优化工具。用户上传一份可选中文本的 PDF 简历，并填写目标岗位描述（JD）；系统结合两份材料生成岗位匹配度、简历优化建议、定制求职信与面试题参考答案，同时将本人历史分析安全保存，便于持续迭代求职材料。

产品的核心价值不是替用户虚构经历，而是帮助用户把已有经验组织成更贴近目标岗位、可验证且更有说服力的表达。

## 2. 产品目标与边界

### 2.1 产品目标

1. 降低求职者针对不同岗位修改材料的时间成本。
2. 用结构化反馈说明“已经匹配什么、还需补足什么、如何改写”。
3. 为同一份简历沉淀可回看的岗位分析历史。
4. 让用户在本地预览、登录保存、历史复盘之间获得连续体验。

### 2.2 v1 范围

- 邮箱与密码注册、登录、退出及忘记密码。
- PDF 简历上传与浏览器端文字提取。
- 目标岗位描述输入。
- DeepSeek 驱动的单次岗位分析。
- 分析结果、简历文件和岗位描述的个人历史保存与查看。
- 响应式中文网页体验，以及线上 Netlify 部署。

### 2.3 非目标

- 不替用户自动投递职位或抓取招聘网站。
- 不承诺面试、录用或特定匹配分数。
- 不支持扫描件、图片型 PDF 的 OCR；当前仅支持可选中文本的 PDF。
- 不提供团队协作、付费订阅、职位订阅和账号注销；这些属于后续版本。

## 3. 用户与使用场景

| 用户 | 典型诉求 | 产品价值 |
|---|---|---|
| 正在投递的求职者 | 针对一份 JD 快速调整简历与求职信 | 用明确建议缩短准备时间。 |
| 转岗或应届求职者 | 判断已有经验和目标岗位的差距 | 获得能力覆盖、待补足项和面试准备方向。 |
| 多岗位投递者 | 回看不同岗位的准备结果 | 保存分析档案，避免重复整理材料。 |

## 4. 用户端工作流程

### 4.1 首次使用与材料准备

1. 用户进入首页，看到示例岗位描述与示例报告；示例明确标注，可直接覆盖。
2. 用户拖拽或点击上传 PDF 简历。页面校验格式、文件大小与可提取文字，并显示文件名、大小和解析状态。
3. 用户填写或粘贴目标岗位描述。岗位描述长度需为 20–20,000 字。
4. 未登录用户可以阅读示例与准备材料；点击分析时会被引导先登录或注册。

### 4.2 账户流程

1. 新用户在账户弹窗输入邮箱、至少 8 位密码及确认密码，创建账户后即可使用。
2. 已注册用户使用邮箱和密码登录；登录成功后可保存简历并访问“我的分析”。
3. 用户忘记密码时输入注册邮箱，系统发送密码重置链接；用户在链接页面设置新密码后回到网站。
4. 用户可随时退出，退出后受保护的保存、分析与历史能力均不可用。

当前产品策略为“注册后直接登录”，不要求邮箱确认。Supabase 仍负责密码重置邮件；正式运营时应配置自定义 SMTP，避免默认邮件服务的频率限制。

### 4.3 一次完整分析

1. 登录用户点击“开始深度分析”。
2. 系统先将原始 PDF 与提取出的简历文字安全保存到个人账户。
3. 系统基于已保存简历和当前 JD 执行 AI 分析，页面显示处理中状态。
4. 完成后在当前页展示完整报告：
   - 岗位匹配度与能力覆盖说明；
   - 核心洞察、优势与待补足标签；
   - 3 条按优先级排列的简历优化建议，含原表述与推荐改写；
   - 针对岗位的求职信；
   - 10 个面试问题及参考回答。
5. 用户可复制求职信；落款日期由系统按中国时区自动生成。
6. 用户进入“我的分析”查看历史列表，点击任一记录进入对应详情。

### 4.4 材料连续性

- 当前浏览器会以 IndexedDB 暂存正在准备的 PDF、提取文字、JD 和已保存简历标识。
- 用户从首页进入历史页再返回时，材料会恢复，可继续分析。
- 草稿按当前浏览器和当前用户隔离；不登录时属于访客草稿，登录后不会泄露给其他账号。

## 5. 网页实际工作流程

```text
首页输入区
  ├─ 浏览器：校验 PDF → pdfjs-dist 提取文字 → 暂存 IndexedDB
  ├─ 浏览器：维护 JD、登录状态与页面报告状态
  └─ 用户点击分析
       ↓
Next.js /api/resumes
  ├─ 通过 Supabase SSR Cookie 验证当前用户
  ├─ 校验 PDF（PDF、≤20 MB）与提取文字（30–60,000 字）
  ├─ 上传私有 Storage：resume-files/{user_id}/{resume_id}.pdf
  └─ 写入 resumes（文件元数据、解析文字、解析状态）
       ↓
Next.js /api/analyze
  ├─ 再次验证 SSR Cookie 与简历归属
  ├─ 写入 job_descriptions、analysis_runs 与 usage_events
  ├─ 使用服务端环境变量调用 DeepSeek Chat Completions
  ├─ 校验并规范化 JSON 结果
  ├─ 写入 analysis_results，更新任务为 completed / failed
  └─ 返回结构化报告与完成时间
       ↓
网页报告 / 历史详情页
  ├─ 统一排版展示 01–05 模块
  ├─ 系统补充中国时区日期、清理旧结果的日期和“参考”前缀
  └─ 仅允许当前用户读取自己的历史记录
```

### 5.1 认证与会话

- 前端通过站内认证接口完成登录、注册、退出和密码重置。
- Next.js 中间件使用 Supabase SSR Cookie 会话：每个请求创建服务端客户端、调用 `getClaims()` 验证/刷新会话，并把更新后的 Cookie 回写浏览器。
- `/api/resumes`、`/api/analyze` 和历史页面只信任请求 Cookie，不使用前端内存 Token 或 `Authorization` Bearer 回退。
- 导航栏的账户信息与受保护接口采用同一会话来源，避免“界面显示已登录、接口却返回 401”。

### 5.2 AI 结果规则

- 模型只返回 JSON，不返回 Markdown。
- 分数为 0–100 的整数；建议固定 3 条；面试题固定 10 条。
- 提示词禁止编造简历中不存在的经历或数字；信息不足时必须说明待补充项。
- 求职信由模型提供正文与署名，不允许包含日期；页面统一补充分析完成日期。
- 面试回答禁止包含“参考”“参考回答”等前缀；渲染时也会兼容清理旧数据，保证页面每题仅出现一次“参考：”。

## 6. 页面与交互要求

| 区域 | 主要内容 | 行为与规则 |
|---|---|---|
| 顶部导航 | 品牌、登录/账户、我的分析 | 未登录显示“登录并保存”；已登录显示账户和退出入口。 |
| 首屏介绍 | 产品定位、说明文案 | 桌面端主标题保持一行；窄屏自动换行。 |
| 简历上传区 | PDF 上传、文件信息、解析状态 | 支持点击和拖拽；仅接受 PDF，最大 20 MB。 |
| 岗位描述区 | JD 文本框、字数与示例提示 | 初始示例标注“举例”和“可直接覆盖”。 |
| 分析按钮 | 开始深度分析 | 材料不完整、解析中或未登录时不可进入真实分析。 |
| 01/02 报告区 | 匹配分、摘要、洞察、标签 | 必须显示 `01 / MATCH ANALYSIS` 和 `02 / WHAT WE SEE` 编号。 |
| 03 报告区 | 简历优化建议 | 原表述、建议表述与 02 正文统一为 14px 无衬线正文。 |
| 04 报告区 | 求职信、复制 | 不显示模型自带旧日期；使用本次完成日期。 |
| 05 报告区 | 10 条面试问答 | 每条仅展示一个“参考：”标签。 |
| 历史页与详情 | 历史列表、历史报告 | 列表卡可进入 `/history/[runId]`；无权访问时回到列表，不泄露记录。 |

## 7. 数据、权限与安全

### 7.1 数据模型

使用 Supabase Auth 内置 `auth.users`，业务表包括：

| 表 / 服务 | 保存内容 | 权限原则 |
|---|---|---|
| `profiles` | 显示名称、目标岗位偏好、引导状态 | 与 Auth 用户一对一。 |
| `resumes` | 简历元数据、解析文字、解析状态 | 用户仅管理自己的简历。 |
| `job_descriptions` | 岗位标题、公司/地点（预留）、JD 原文 | 用户仅管理自己的岗位描述。 |
| `analysis_runs` | 一次任务的输入快照、模型、状态与错误信息 | 仅当前用户可读；服务端写入。 |
| `analysis_results` | 匹配分和完整结构化结果 | 与分析任务一对一，仅当前用户可读。 |
| `usage_events` | 请求、成功、失败事件 | 用于后续额度和运营统计，仅当前用户可读。 |
| 私有 Bucket `resume-files` | 原始 PDF | 路径为 `{user_id}/{resume_id}.pdf`，只能访问本人目录。 |

所有业务表均启用 RLS，并以 `user_id = auth.uid()` 或等价关联隔离数据。删除用户时，外键级联删除业务记录；简历保存失败时会清理已经上传但未写入数据库的文件。

### 7.2 密钥与环境变量

| 环境变量 | 用途 | 安全要求 |
|---|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | 浏览器与服务端连接 Supabase 项目 | 可公开，但必须指向正确项目。 |
| `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` | 浏览器侧 Supabase 公共访问 | 可公开；受 RLS 保护。 |
| `SUPABASE_SECRET_KEY` | 服务端保存文件、创建分析记录 | 仅服务端；绝不使用 `NEXT_PUBLIC_` 前缀。 |
| `DEEPSEEK_API_KEY` | 服务端调用 DeepSeek | 仅服务端；不得进入 Git、浏览器或截图。 |
| `DEEPSEEK_BASE_URL` | DeepSeek API 根地址 | 默认使用官方 API 根地址。 |
| `DEEPSEEK_MODEL` | 使用的模型名称 | 未设置时使用代码默认模型。 |

本地值放在 `.env.local`，线上值配置在 Netlify 环境变量。仓库只保留不含真实密钥的 `.env.example`。

## 8. 技术架构与部署

| 层级 | 方案 | 职责 |
|---|---|---|
| 前端与服务端 | Next.js 15 + React 19 + TypeScript | 页面、路由、服务端 API 与 SSR。 |
| PDF 解析 | `pdfjs-dist` | 在浏览器提取可选中文本。 |
| 认证与数据 | Supabase Auth、Postgres、Storage、RLS | 账户、私有简历文件、历史和权限。 |
| AI | DeepSeek OpenAI 兼容接口 | 生成结构化岗位分析。 |
| 部署 | Netlify + `@netlify/plugin-nextjs` | 执行构建并托管 Next.js 应用。 |

### 8.1 本地运行

1. 基于 `.env.example` 创建 `.env.local` 并填写必需环境变量。
2. 执行 `npm install`。
3. 执行 `npm run dev`。
4. 固定从 `http://127.0.0.1:3000` 打开，避免与 `localhost` 混用。

### 8.2 线上部署基线

1. Netlify 使用 `npm run build` 和 `@netlify/plugin-nextjs` 构建。
2. 将 Supabase 与 DeepSeek 所需变量写入 Netlify 项目环境变量，再触发生产部署。
3. 将 Netlify 项目设为 Public，确保访客可访问站点。
4. 在 Supabase **Authentication → URL Configuration** 设置生产 Site URL，并将生产域名的 `/auth/callback` 加入 Redirect URLs；本地 `127.0.0.1` 回调地址可同时保留用于开发。
5. 部署后分别验证首页样式、注册/登录、保存简历、分析、历史详情与密码重置链接。

## 9. 从建立到部署的主要问题与解决方式

| 阶段 | 问题与现象 | 根因 | 已采用的解决方式 |
|---|---|---|---|
| 静态视觉稿 | 01、02 模块编号缺失；卡片边界/强调线不一致 | 视觉结构没有把编号与卡片内容作为稳定元素处理 | 固定 `01 / MATCH ANALYSIS`、`02 / WHAT WE SEE` 等分区编号；统一卡片边界、间距和强调样式。 |
| 视觉落地 | 03、04 正文与 01、02 字号字体不一致；求职信日期陈旧 | 不同模块使用了独立文本样式，模型会自行生成日期 | 统一正文为 14px 无衬线与一致行高；提示词禁止日期，前端按中国时区补充实际完成日期。 |
| AI 内容 | 面试题出现两个“参考” | 模型答案自带前缀，页面又固定输出前缀 | 同时限制提示词和渲染清理逻辑，最终只保留一个“参考：”。 |
| 历史体验 | 历史记录只能看列表，返回首页后材料消失 | 列表缺少详情路由；首页状态仅在 React 内存中 | 新增 `/history/[runId]` 详情页；用 IndexedDB 按用户暂存材料草稿。 |
| 会话同步 | 顶栏显示已登录，保存简历却提示“请先登录” | 客户端用户状态、Bearer Token 与 SSR Cookie 三条认证链路并存；中间件未完整回写刷新 Cookie | 统一为 Supabase SSR Cookie；中间件调用 `getClaims()` 并回写 Cookie；接口只从请求 Cookie 验证用户。 |
| 本地认证 | 注册统一失败，或登录/回调状态异常 | 本地服务访问 Auth 的网络受限；`localhost` 与 `127.0.0.1` 混用使 Cookie 与回调来源不一致 | 恢复可联网的开发服务；本地地址统一为 `http://127.0.0.1:3000`，同步更新 Site URL 与 Redirect URLs。 |
| 邮件流程 | 确认邮件过期、重置邮件限流、用户无法继续注册 | 反复重发确认邮件触发 Supabase 默认 SMTP 频控；产品策略与 Dashboard 设置不一致 | 现阶段关闭 Confirm email，注册后直接登录；忘记密码保留邮件；正式运营配置自定义 SMTP。 |
| 用户资料 | `profiles` 表看起来为空 | 旧用户创建早于资料触发器或尚无新的注册事件 | 保留 `auth.users` 创建 profile 的触发器，并对历史用户做一次安全补齐；空 profile 不会影响基础登录，但会影响个人资料扩展。 |
| Netlify 首次发布 | 上线后出现旧的静态页、没有登录入口 | 根目录遗留 `index.html` 被静态部署优先作为首页 | 将旧视觉稿改为 `static-preview.html`，根路径由 Next.js 应用接管。 |
| Netlify 样式 | 页面只有裸文字，`/_next` CSS 404 | 手工发布 `.next` 不包含 Next 所需的完整静态资源与运行时处理 | 使用 `netlify.toml` 中的 Next.js 插件，并用 `netlify deploy --prod` 让 Netlify 完整构建与发布。 |
| Netlify 访问 | 网站部署成功但访客无法使用 | 新账户项目默认可见性为 Private | 在 **Project configuration → Access & security → Visitor access** 设为 Public。 |
| Netlify 额度 | 原账户无法继续部署 | 账户信用额度已耗尽，平台拒绝新部署 | 切换到可用账户重新关联站点与环境变量；删除旧部署不会恢复已经消耗的额度。 |
| GitHub 命令行授权 | 浏览器已确认，`gh auth login` 仍失败 | 本机命令行到 GitHub 的 HTTPS / 代理端口不可达 | 使用正常工作的本地代理端口后重新进行 device login；用 `gh auth status` 验证，而不是仅以网页成功页判断。 |

认证详细排查过程见：[认证问题与排查说明.md](./认证问题与排查说明.md)。邮箱认证配置见：[Supabase认证配置.md](./Supabase认证配置.md)。

## 10. 验收标准

### 功能验收

- [ ] 新用户可用至少 8 位密码注册并直接登录。
- [ ] 已登录用户上传可选中文本 PDF 后可成功保存，文件大小限制为 20 MB。
- [ ] 输入合格 JD 后可完成一次分析，并显示四类完整结果。
- [ ] 结果中保留 01–05 模块编号；03、04 正文排版与 02 正文一致。
- [ ] 求职信日期为本次分析完成日（中国时区），面试题每题仅有一次“参考：”。
- [ ] “我的分析”可以打开列表和任意本人历史详情；非本人记录不能访问。
- [ ] 返回首页后当前浏览器的材料草稿仍可恢复。
- [ ] 退出后，保存、分析和历史访问都明确要求登录。

### 安全与部署验收

- [ ] 数据库表和私有 Storage 都启用并正确执行 RLS。
- [ ] 浏览器端、仓库与文档均不含 Secret Key、DeepSeek Key、密码或会话 Token。
- [ ] 线上环境变量已配置，生产页面样式与登录入口正常。
- [ ] Supabase Site URL 和 Redirect URLs 同时包含当前生产域名的认证回调地址。
- [ ] `npm run build` 通过，生产站点可直接访问。

## 11. 后续迭代建议

1. 接入自定义 SMTP，并完善品牌化密码重置邮件。
2. 增加简历版本管理、职位公司信息编辑与历史筛选。
3. 设计分析额度、订阅、支付和管理员运营面板。
4. 增加 PDF OCR 或文档格式支持，并提供解析质量提示。
5. 为 AI 结果增加人工编辑、导出和版本对比能力。
6. 接入线上错误监控、分析调用耗时与失败率监控。
