# 认证问题与排查说明

> 记录日期：2026-08-15
> 范围：本地开发环境 `http://127.0.0.1:3000` 的 Next.js + Supabase 邮箱密码认证、会话同步、简历保存与分析请求。
> 安全说明：本文不包含 Supabase URL、API Key、用户邮箱、密码、访问令牌或任何其他敏感信息。

## 一、问题概览

本轮排查中，认证相关问题并非单一原因造成，而是三类问题叠加：

| 分类 | 用户看到的现象 | 最终结论 |
|---|---|---|
| 服务端会话不同步 | 顶部已显示用户名，但保存简历时提示“请先登录后再保存简历”；“我的分析”被跳回首页。 | 浏览器客户端状态与 Next.js 服务端 Cookie 会话没有统一。 |
| 本地服务网络受限 | 注册时统一提示“账户暂时无法创建，请稍后再试”。 | 当时运行的本地 Next.js 开发进程无法连接 Supabase Auth，错误被映射为通用注册失败。 |
| 邮件确认与限流 | 确认链接过期、忘记密码/注册收不到邮件、提示验证邮件发送过于频繁。 | 旧的“必须验证邮箱”流程留下了过期链接；Supabase 默认 SMTP 有共享发送限额。 |

这些问题不表示数据库 RLS、Storage 权限或简历 PDF 本身失效。日志证据表明，早期“保存简历失败”在到达数据库操作前就因认证失败返回了 `401`。

## 二、用户侧出现过的现象

1. 已登录后，页面顶栏显示用户名，但点击保存简历仍提示“请先登录后再保存简历”。
2. 访问“我的分析”后被重定向至首页，并带有 `auth=required` 一类参数。
3. 原有账号登录时出现“登录会话已失效，请重新登录”或密码不正确。
4. 注册和忘记密码无法收到新邮件，或收到的确认链接提示 `otp_expired`。
5. 使用新邮箱注册时，出现“验证邮件发送过于频繁，Supabase 已暂时限流”。
6. 后续注册曾显示“账户暂时无法创建，请稍后再试”。

## 三、根因分析

### 1. 浏览器状态不等于服务端认证状态

旧实现同时保留了多条认证链路：React 客户端中的用户状态、请求中的 Bearer Token 回退，以及 Next.js/Supabase SSR Cookie。页面顶栏只要拿到客户端用户对象就会显示用户名；但 `/api/resumes`、`/api/analyze` 和服务端历史页面需要可验证的 Cookie 会话。

因此出现了“界面像是已登录，但接口返回 `401`”的矛盾状态。该问题与数据库权限无关：接口在开始读写数据库前便已拒绝请求。

### 2. SSR 中间件没有完整回写刷新后的会话 Cookie

Supabase SSR 的会话刷新需要在每个请求中创建独立服务端客户端，紧接着调用 `getClaims()`，并将 SDK 返回的 Cookie 与相关响应头完整写回浏览器。若创建新的响应对象却没有保留这些 Cookie/响应头，浏览器与服务端会逐渐持有不同的会话状态。

这正是会话“看似存在、实际被服务端视为失效”的直接技术原因之一。

### 3. `localhost` 与 `127.0.0.1` 混用会分裂 Cookie 与回调来源

`localhost:3000` 与 `127.0.0.1:3000` 对浏览器而言是不同站点。它们的 Cookie、Supabase Auth 回调白名单和 Site URL 都是独立的。曾经混用两种地址会让已有会话与邮件回调落到不一致的来源。

本项目现统一使用：`http://127.0.0.1:3000`。

### 4. 本地开发服务当时没有访问 Supabase Auth 的网络能力

注册接口最初的服务端诊断日志显示：

```text
[auth][signup] rejected { status: 0, code: 'unknown' }
```

`status: 0` 并非正常的 Supabase 业务错误码，说明请求没有成功抵达 Auth 服务。进一步验证显示：受限环境中的 Node HTTPS 请求连接 Supabase 时被拒绝；在具有正常网络权限的服务进程中，同一连通性请求可以得到预期的 Supabase HTTP 响应。

因此，“账户暂时无法创建”当时是本地服务网络异常被前端友好文案覆盖后的表现，而不是“新用户注册”或数据库触发器本身出错。

### 5. 邮箱确认流程切换与默认 SMTP 限流

早期方案要求新用户点击邮箱确认链接。确认邮件可能因重复发送而过期；默认 SMTP 还存在发送频率限制，并可能按项目共享计数。反复尝试注册、重发确认或重置密码，都会更容易触发限流。

之后的产品决策改为：允许邮箱+密码注册后直接登录，不要求邮箱确认。此设置必须同时在 Supabase Dashboard 关闭 **Confirm email**，不能只修改页面代码。

## 四、排查流程与证据

| 步骤 | 检查内容 | 结论 |
|---|---|---|
| 1 | 检查 `/api/resumes` 响应与服务端日志。 | 请求早期返回 `401`，尚未进入简历数据或 Storage 操作。 |
| 2 | 对照顶栏用户名与 `/api/auth/session`、历史页行为。 | 客户端显示用户不代表服务器已验证 Cookie 会话。 |
| 3 | 审查中间件、路由处理器与前端认证代码。 | 发现 Cookie、客户端状态和 Bearer 回退链路并存，且会话刷新回写不完整。 |
| 4 | 检查 Supabase Auth 日志。 | 邮箱密码登录曾成功；存在确认链接过期和默认 SMTP 限流记录。 |
| 5 | 检查用户资料触发器及 `profiles` 表约束。 | 新用户资料触发器仅创建基础 profile，不会阻断注册。 |
| 6 | 比较本地受限进程与正常网络环境下的 Supabase Auth 连通性。 | 锁定注册通用失败的直接原因是开发服务无法联网。 |
| 7 | 在可正常联网的本地开发进程中重新运行网站。 | 后续日志出现 Cookie 会话验证、保存简历和 AI 分析均成功的记录。 |

最后一项成功日志的关键特征如下（已省略任何用户信息）：

```text
[auth] Cookie 会话已验证 { path: '/api/resumes' }
POST /api/resumes 200

[auth] Cookie 会话已验证 { path: '/api/analyze' }
POST /api/analyze 200
```

这证明修复后的完整路径已通过：Cookie 会话验证 → 简历保存 → 分析请求。

## 五、已实施的修复

### 认证架构

- 认证统一为 Supabase SSR Cookie 会话。
- 移除了 React `accessToken` 状态、手工 `Authorization: Bearer ...` 请求头及 Bearer 回退验证，避免多个会话来源互相矛盾。
- 登录、注册、退出、保存简历和发起分析统一走服务端路由与 Cookie。
- 前端导航栏通过服务端验证后的会话状态显示用户信息，而不是单独相信内存状态。

### Next.js 与 Supabase SSR

- 中间件为每个请求创建独立 Supabase 服务端客户端。
- 紧接着使用 `supabase.auth.getClaims()` 验证和刷新会话。
- 将刷新后的 Cookie 以及 Supabase SSR 所需响应头完整回写给浏览器。
- 受保护接口只从请求 Cookie 验证用户；无会话时明确返回 `401`。
- 增加不含令牌、密码、邮箱等敏感信息的诊断日志，例如“Cookie 会话缺失 / 已验证 / 验证失败”。

### 本地地址与账户流程

- 本地地址统一为 `http://127.0.0.1:3000`。
- Supabase Dashboard 的 Site URL 和 Redirect URL 应与该地址保持一致；开发时不要再混用 `localhost`。
- 密码前端校验与 Supabase Dashboard 均使用至少 8 位。
- 当前产品配置采用注册后直接登录的方式；Dashboard 中应保持 Email Provider 与 User signups 开启，并关闭 Confirm email。
- 本地开发服务已在能够访问 Supabase 的正常进程中重新启动。

## 六、当前验证结果

| 验证项 | 结果 |
|---|---|
| 服务端读取 Cookie 会话 | 通过：`/api/auth/session` 返回已认证状态。 |
| 登录 | 通过：有效账号的登录路由返回 `200`。 |
| 保存简历 | 通过：`/api/resumes` 在已验证会话下返回 `200`。 |
| 发起分析 | 通过：`/api/analyze` 在已验证会话下返回 `200`。 |
| 查看历史 | 通过：历史页在登录会话存在时可访问。 |
| 退出登录 | 通过：退出接口返回 `204`，后续请求不再携带有效会话。 |

## 七、后续使用与排障规范

1. 始终从 `http://127.0.0.1:3000` 打开本地网站；如果页面表现异常，先执行浏览器强制刷新（`Ctrl + F5`）。
2. 已存在的邮箱应使用“登录”，而不是重复“注册”。重复注册不用于恢复密码，也可能触发邮件发送限制。
3. 只有在保留邮箱确认的产品策略下，才需要配置确认邮件模板与 `/auth/confirm` 回调。当前直接登录策略下，不应再反复发送确认邮件。
4. 忘记密码依旧需要发送邮件；如果继续使用 Supabase 默认 SMTP，遇到限流时应等待限额恢复。正式产品应配置自定义 SMTP。
5. 若再次出现“顶栏已登录但接口 401”，优先查看新开发服务日志中的 `/api/auth/session` 与 `[auth] Cookie 会话...` 记录，再检查是否从 `127.0.0.1` 打开。
6. 不要在客户端代码、Git 仓库、截图或本说明文档中写入 Secret Key、service role key、访问令牌、密码或完整会话 Cookie。

## 八、未执行或未改动的项目

- 未删除或重置任何 Supabase 用户账号。
- 未修改既有业务表、RLS 策略、Storage 文件或历史分析数据。
- 未通过测试账号执行实际注册，避免创建未经用户授权的 Auth 用户。
- 未更改任何用户密码。

## 九、建议的回归测试清单

- [ ] 新邮箱、8 位以上密码注册后可立即进入登录状态。
- [ ] 刷新页面后，顶栏账户状态、保存简历权限和“我的分析”访问结果一致。
- [ ] 上传 PDF 并保存，接口不再返回 `401`。
- [ ] 发起分析后，结果与历史记录可查看。
- [ ] 退出后，顶栏、受保护页面和接口均一致表现为未登录。
- [ ] 使用错误密码时，显示登录失败，但不会误显示为已登录。
- [ ] 如启用忘记密码，确认邮件发送和重设流程在 SMTP 配额充足时可正常完成。

## 十、相关文件

- [中间件](./middleware.ts)：请求期间刷新并回写 Supabase Cookie 会话。
- [会话刷新实现](./lib/supabase/proxy.ts)：SSR Cookie 与响应头处理。
- [接口用户校验](./lib/supabase/request-user.ts)：只从 Cookie 验证用户。
- [认证接口目录](./app/api/auth)：登录、注册、退出、会话与密码相关接口。
- [简历保存接口](./app/api/resumes/route.ts)：在写入前验证 Cookie 会话。
- [分析接口](./app/api/analyze/route.ts)：在调用 AI 前验证 Cookie 会话。
